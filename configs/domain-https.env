# Configuration Template: Domain HTTPS Deployment
# Untuk domain dengan HTTPS dan reverse proxy (contoh: https://agent.gantiwifi.online)
# Copy file ini ke config.env dan sesuaikan domain

# Server Configuration  
NODE_ENV=production
PORT=3010
CORS_ORIGIN=https://yourdomain.com

# Production Security
FORCE_HTTPS=true
SECURE_COOKIES=true

# Security Configuration
JWT_SECRET=VouCh3r_W1f1_S3cr3t_K3y_2024_PRODUCTION_HTTPS
SESSION_SECRET=S3ss10n_S3cr3t_K3y_PRODUCTION_HTTPS_2024

# Database Configuration
DB_PATH=./voucher_wifi.db

# Admin Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123_SECURE
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_FULL_NAME=Administrator

# Mikrotik Configuration
MIKROTIK_HOST=192.168.1.1
MIKROTIK_USER=admin
MIKROTIK_PASSWORD=your_mikrotik_password
MIKROTIK_PORT=8728

# WhatsApp Configuration
WHATSAPP_SESSION_PATH=./whatsapp-sessions
WHATSAPP_QR_TIMEOUT=60000

# Admin WhatsApp Numbers
ADMIN_PHONES=628123456789,628987654321

# DEPLOYMENT INSTRUCTIONS:
# 1. Ganti yourdomain.com dengan domain Anda
# 2. Setup nginx/apache reverse proxy ke localhost:3010
# 3. Setup SSL certificate (Let's Encrypt)
# 4. Copy file ini ke config.env
# 5. npm install
# 6. pm2 start backend/server.js --name voucher-wifi
# 7. Akses: https://yourdomain.com

# NGINX REVERSE PROXY EXAMPLE:
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com;
#     
#     ssl_certificate /path/to/cert.pem;
#     ssl_certificate_key /path/to/key.pem;
#     
#     location / {
#         proxy_pass http://localhost:3010;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
